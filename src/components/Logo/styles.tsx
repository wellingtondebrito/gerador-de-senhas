import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: 300,
    height: 100,
    marginBottom: 16,
    justifyContent: 'center',
    alignItems: 'center'
  },
  textHeading: {
    fontSize: 24,
    fontWeight: '700'
  },
  text: {
    fontSize: 16,
  }
});