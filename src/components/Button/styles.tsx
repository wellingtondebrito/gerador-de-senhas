import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "#245596",
    width: 250,
    height: 40,
    padding: 8,
    marginBottom: 8,
    justifyContent: "center",
    alignContent: "center",
    borderRadius: 4,
    alignItems: 'center'

  },
  text: {
    color: "#fff",
    fontSize: 16,
  }
});