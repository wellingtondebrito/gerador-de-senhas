import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {},
  textField: {
    borderColor: "#202020",
    borderWidth: 1,
    width: 250,
    marginBottom: 8,
    padding: 8,
    fontSize: 16,
    borderRadius: 4,
  }
});